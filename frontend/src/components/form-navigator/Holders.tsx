import React from "react";

export const Holder1 = (props: {element: any;}) => {
    return <div>{props.element}</div>;
}

export const Holder2 = (props: {element: any;}) => {
    return <div>{props.element}</div>;
}
